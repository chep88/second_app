<p id="notice"><%= notice %></p>

<legend>Students</legend>

<% @students.each do |student| %>
  <div class="col-lg-4">
    <h4><%= student.name %>, <%= student.age %> Años </h4>

    <ol class="breadcrumb">
      <li><%= link_to 'Show', student, class:"btn btn-info" %></li>
      <li><%= link_to 'Edit', edit_student_path(student), class:"btn btn-success" %></li>
      <li><%= link_to 'Destroy', student, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger" %></li>
    </ol>
  </div>
<% end %>

<br>

<%= link_to 'New', new_student_path, class:"btn btn-info" %>
