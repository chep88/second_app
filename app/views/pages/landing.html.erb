<legend><h1>Estudiantes</h1></legend>
<div class="col-lg-6">
  <table class="table table-striped table-hover ">
    <thead>
      <tr>
        <th>Nº</th>
        <th>Nombre</th>
        <th>Edad</th>
        <th>Correo</th>
        <th>Fecha Creacion</th>
      </tr>
    </thead>
    <tbody>
      <% @asdasdsad.each_with_index do |student, index| %>
      <tr>
        <td><%= index+1 %></td>
        <td><%= student.name %></td>
        <td><%= student.age %></td>
        <td><%= student.email %></td>
        <td><%= student.created_at.strftime("%m/%d/%Y") %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
